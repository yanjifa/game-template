export declare const EditMode: {
    /**
     * 标记编辑器进入了一种编辑模式
     * @param mode 编辑模式的名字
     */
    enter(mode: string): any;
    /**
     * 获取一个类型选中的所有元素数组
     * @param type
     */
    getMode(): string;
};
